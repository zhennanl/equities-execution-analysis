# Variable Lab — Leaderboard (run 1: 16 events, 83 name-events)

*Session 9i. Verdicts assigned MECHANICALLY by the acceptance
criteria locked in VARIABLE_LAB_REGISTRY.md before evaluation.
Panel: 13 FTSE + 3 MSCI TW events, five pillars, PIT by
construction. MSCI cells are all DATA-GATED (3 events < the 6-event
minimum) — verdicts below are FTSE-cell unless noted. Effects =
event-clustered mean of (above-median group − below-median group)
on the target; positive remaining-drift = more favorable drift
still to come after the decision day.*

## ADOPTED (2)

**H2 — Crowding build (deletes), +149 bps, winrate 0.67, LOO-stable
(pooled +171).** BUT the measured DIRECTION is OPPOSITE the
pre-declaration: we declared "high build → covering bounce → less
favorable remaining drift"; the data says high-build deletes kept
PRESSING into the print — more favorable drift remained for the
obligated seller. This is what pre-registration is for: the rule is
adopted WITH the measured sign, and the prior narrative (crowded
deletes recover early) is corrected. Note the coherence: this
matches the CN/HK press-to-print finding and the May-26 TW deletes;
the 6919 squeeze remains the tail case (EXITING/H7 was meant to
catch it — data-gated, only a handful of EXITING observations).
Integration: discretion matrix's crowded-delete row keeps
WORK-AHEAD, now with a measured +149 bps basis replacing narrative.

**H5 — Cohort dispersion, +210 bps, winrate 0.67, LOO-stable —
in-class (FTSE) only; pooled flips sign.** Direction again opposite
the declaration: we declared laggard-convergence; the data says
LEADERS keep leading within FTSE windows (cross-sectional momentum
persistence). MSCI cell (data-gated, n=3) leans the other way —
consistent with the class-inversion history, so this is
STRICTLY-FTSE until the MSCI cell reaches 6 events. Integration:
within a FTSE basket, work laggards EARLY and let leaders run to
the print — an ordering rule the window planner can consume.

## REJECTED (3)

**H3 — A+3 momentum: FTSE −73 bps, winrate 0.38.** The formal
in-class re-test FAILS on the 13-event panel — this REVERSES the
earlier 6-event impression that momentum persisted in-class (the
OOS failure on MSCI was already known). The A+3 gate in
asof_step2/discretion inputs is hereby DEMOTED to descriptive
context; H5's cohort-relative momentum is the surviving (stronger,
event-clustered) form. The lesson: the gate was built on 6 events
and looked fine; 13 events killed it. Effective n is events.

**H1 — Front-run completion (cumulative excess volume): sign
unstable (FTSE wr 0.54; pooled flips).** The intuitive "obligation
already pre-traded → close part-spent" story does not survive
event clustering at daily resolution. Possibly real at intraday
resolution (untestable historically — forward archive from Aug-11).

**H4 — Foreign coverage: sign unstable (wr 0.50-0.54).** TWT38U
cumulative foreign flow at k=5 does not usefully predict remaining
drift — foreign flow is CONFIRMATORY (it validates flow estimates
post-hoc, per the flow-validation studies) not PREDICTIVE mid-window.

## CRITERIA GAP (1) — registry v2 item, not a verdict

**H6 — Early volume surge → print t_mult.** The acceptance
thresholds were written in bps; H6's target is in t_mult UNITS —
the mechanical NULL (|1.9| < 25) is a unit artifact, not a finding.
Observed pattern (reported, unverdicted): high early volume →
print multiple HIGHER by ~2x with winrate 0.69-0.75 — early volume
looks like event-intensity signal, not flow-pulled-forward.
Criteria cannot move post-hoc: H6 re-enters registry v2 with a
t_mult-unit threshold, graded on the next run.

## DATA-GATED (2)

**H7 EXITING flip** (too few EXITING cases) and **H8 borrow
utilization** (quota coverage thin — never evaluated). Both stand
in the registry; coverage grows each event.

## Standing out-of-sample

Aug-2026 grades every verdict above on fresh data. MSCI cells
unlock at 6 events (~Nov-2027 at current cadence, sooner with
alias-bridged history).
