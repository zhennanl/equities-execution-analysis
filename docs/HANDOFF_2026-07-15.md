# HANDOFF / CONTEXT CAPSULE — 2026-07-15

*Full context export of the working chat. Supersedes HANDOFF_2026-07-08_v3.md
for platform state; the v2/v3 operating protocols (P-A…P-G) still apply.
Paste this file (or attach it) into a new chat to continue seamlessly.*

---

## 1. Who / what / why

- **User:** Bill (Zhennan Luo), billluokd@gmail.com.
- **Goal:** interview prep for a **GSET (Goldman Sachs Electronic Trading)
  Quantitative Execution Consultant / Execution Solutions** role. Round-1
  feedback: round 2 is **heavily statistics-focused**. Likely interviewer
  "Sherry": Zhejiang econ → GS Hong Kong 2012–14 → Tokyo 2014–17 → senior
  APAC Execution Solutions ("Execution advisory and solutions coverage for
  APAC electronic trading, responsible for improving ranking of algo
  performance for diverse client base").
- **Vehicle:** the **Execution Analytics Platform** — Python/Streamlit,
  free yfinance data, agentic architecture, at
  `C:\Users\Bill\Downloads\execution_analytics`
  (sandbox path `/sessions/<session>/mnt/execution_analytics`).

## 2. Operating mode (standing instructions)

- **Autopilot:** make decisions independently, don't ask; deep research OK.
- **Git:** user reviews & commits himself — assistant NEVER commits.
  Recurring chores: `rm -f .git/index.lock`, `touch` changed files +
  `git update-index --really-refresh` so git status shows real state.
  ⚠️ Verify the COMMITTED `tests/test_sell_side.py` is the fixed copy (a
  corrupted trailing line resurfaced once from a broken committed HEAD).
- **Protocols P-A…P-G:** anchored bash-python patches with asserts-before-
  write for big files; backups to `~/backups/`; trailing dataclass fields
  need defaults; buy/sell mirror invariant tested; minimal yfinance
  fetches; keep offline pytest suite green.
- **Culture:** "when in doubt between impressive and honest, choose
  honest." Every model gated vs a naive baseline ("a model that can't beat
  the 20-day median ships the 20-day median"). Every doc carries an honest
  boundaries section.
- **Session log:** append to `docs/SESSION_SUMMARY_2026-07-08.md` each
  work block (currently through session 6g).

## 3. Platform state (suite: **229 passed, 1 live-marked deselected**)

Four Streamlit modules (`app.py` → `views/` package; NOT `pages/` — that
name conflicts with Streamlit auto-nav):

1. **📈 Execution Algorithm Simulator** (`views/page1_simulator.py`) —
   8 algos (VWAP/TWAP/POV/IS-Almgren-Chriss/MOC/MOO/LIQ/STEALTH) with two
   publicly-documented GSET traits adopted (POV outsized-print cap 3×
   12-bar median; LIQ "Liquidity Shield" behind-schedule relaxation);
   orchestrated agent pipeline (regime, cost model, pre/post-trade TCA
   with Perold IS attribution reconciling ±0.1bp, markouts, critic that
   flags-never-overrides); pre-trade desk pack + run library (predicted vs
   realized learning loop); live volume run-rate re-forecast; venue router
   / SOR (Agent 13, 5 policies incl. **"Shield (dark-patient)"** —
   cross-bar dark-residual carry for the first 50% of schedule,
   conservation tested); **counterfactual impact propagator** (Sherry's
   interview question: perm/temp kernel, causal, η×half-life grid
   sensitivity bands with robust/needs-A/B verdict); **flow-forecast
   layers L1–L6b** (AR+calendar DM-gated vs 20-day median; Kalman-lite
   blend; close-share AR(1); event uplift; BVC diagnostics declared
   non-alpha; numpy-ridge ML gate; L6b quantile regression P10/P50/P90
   pinball-gated vs empirical quantiles + pooled cross-sectional ridge
   gated vs per-name AR).
2. **🔄 Index Rebalancing Analysis** (`views/page2_rebalancing.py`) —
   event study with single-firm inference (forecast-error-corrected
   Brown-Warner/BMP), S1–S4 strategy frontier (Madhavan/Petajisto/
   Greenwood-Sammon anchored), trader cards/playbooks/basket mode,
   crowding score, expected move, best-ex record store, event library.
3. **🧺 Program Trading Desk** (`views/page3_program.py`) — sessions,
   regulations, settlement/recon, program blotter (APAC: Taiwan ±10%
   limits, China A T+1, HK CAS/VCM, Japan special quotes, Korea VI).
4. **📋 Quarterly Client Review** (`views/page4_quarterly_review.py`, NEW
   this session) — six-exhibit QBR from the run library: flow profile →
   headline distributions → decomposition (no verdict below n=5) →
   difficulty-adjusted ranking (regression w/ sqrt-size+urgency+market
   FEs; raw-vs-adjusted rank movers = the OVB defense) → outlier Pareto →
   trend & rule-generated recommendations with supporting numbers.
   `synthesize_demo_quarter` plants known structure (tested recovery).

**Market data is source-agnostic by contract** (this session):
`agents/agent1_market_data.assemble_market_data` derives ADV/Yang-Zhang/
profile from any two normalized OHLCV frames. Three sources:
- **yfinance** (default), 5-min bars / 60d daily;
- **kdb+/q** (`agents/kdb_source.py`): KdbSchema table/column mapping,
  server-side `xbar` bar aggregation (ticks never cross IPC), qpython→PyKX
  driver fallback, injectable query_fn (testable w/o server), Page-1
  connection form; honest boundaries in `docs/KDB_INTEGRATION.md` (no
  .u.sub, no sym-enum edges, no pagination, identifier trust domain);
- **tick files** (`agents/tick_ingest.py`): LOBSTER (exec types 4/5,
  ×10000 price), Binance public data (ms/µs epoch auto), generic CSV,
  IEX HIST via optional IEXTools; normalized trades frame = canonical kdb
  trade shape; `trades_to_bars` matches q xbar semantics exactly;
  `to_kdb_csv` + q 3-liner (`trade:("DSTFF";enlist",")0:`$":trades.csv"`)
  for the "real ticks served from my own kdb+" demo. Thin single-day
  ADV/vol context disclosed; optional external daily supplement.

**Free tick data sources vetted (2026-07):** LOBSTER samples
(data.lobsterdata.com — AAPL/MSFT etc. 2012-06-21, instant), IEX HIST
(free T+1 pcap, ~12 months, IEX-only tape ~2% of volume), Nasdaq ITCH
sample days (ftp://emi.nasdaq.com/ITCH/), Binance public data
(data.binance.vision, unlimited crypto tick history), Databento $125
credit (TAQ-quality per symbol-day), HF Data Library (free 1-min OHLCV).

## 4. Statistics arsenal (interview-critical)

DM gates everywhere (Bartlett HAC); Friedman + Nemenyi CD algo wheel;
condition-adjusted ranking via cost ~ algo dummies + controls (HC1) — the
wheel/OVB defense; single-firm event inference w/ forecast-error
correction; power arithmetic (n≈1,760 pairs for 2bps at σ_d=30);
quantile regression via Koenker-Bassett LP (scipy HiGHS) with pinball
gating; pooled ridge w/ fixed effects; Yang-Zhang vol; Roll spread (eps
guard, "undefined (diagnostic)" on trends); BVC; Ljung-Box; propagator
calibration roadmap (NLS on reversion/markouts → Bayesian shrinkage →
bootstrap → sim-to-real regression) in COUNTERFACTUAL_IMPACT_MODEL.md.

## 5. Key docs inventory (`docs/`)

HANDOFF_2026-07-15.md (this file) · SESSION_SUMMARY_2026-07-08.md (running
log, sessions 1–6g) · KDB_INTEGRATION.md · GSET_ALGO_IMPLEMENTATION_NOTES.md
(incl. SOR/dark section + honest not-replicated list) ·
COUNTERFACTUAL_IMPACT_MODEL.md · INDEX_REBALANCE_RESEARCH.md ·
EXECUTION_SOLUTIONS_ANGLE.md · INSTITUTIONAL_PLATFORM_PROPOSAL.md ·
SIMULATOR_INSTITUTIONAL_ASSESSMENT.md · QUANT_REVIEW_ADDITIONS.md ·
ARCHITECTURE_DIAGRAMS.md (+diagrams/*.mermaid) · DEMO_VIDEO_SCRIPT.md v1.1 ·
GSET_Prep_Roadmap v2 (docx/pdf) · five question banks + quiz_src/ build
system · QUESTIONS_FOR_HER.md · HANDOFF_STATS_REVIEW.md ·
CONTEXT_CAPSULE_MOBILE.md · ORIGINAL_TWSE_PROJECT_REVIEW.md ·
Index_Rebalancing_User_Manual.docx v1.1 · Trader_Features_Guide.docx ·
INSTITUTIONAL_GAP_REGISTER.md.

## 6. Interview narrative anchors

- **Counterfactual tape** (Sherry's question): overlay → propagator
  (implemented) → liquidity-consumption; grid sensitivity; "robust across
  kernel grid or it says 'needs a live A/B'."
- **Ranking defense** (her JD line): raw league tables mislead on
  unbalanced flow → condition-adjusted rank; QBR page demos rank movers.
- **House rule:** every forecast layer DM/pinball-gated vs naive;
  ships-the-median honesty.
- **Shield policy:** modeled on the *public* Sonar Dark X description;
  conservation as tested invariant; real Liquidity Shield's counterparty
  scoring honestly not replicated.
- **kdb+:** "pipeline is source-agnostic by contract; kdb+ plugs in behind
  the same MarketData interface with xbar aggregation server-side."
- **QBR:** exhibit 4 = "improving ranking of algo performance" made
  concrete; recommendations only fire with non-overlapping CIs.
- Email to Sherry already sent (appreciative, "plan to continue working on
  the project").

## 7. Known warts / watch items

- `test_integration.py::test_live_fetch_market_data` fails offline (needs
  network) — the `-m "not live"` deselection is the normal run.
- Streamlit deprecation nags: `use_container_width` → `width=` (cosmetic).
- Pre-existing pyflakes widget-var noise in page1 (known/benign).
- Whole `views/` directory + many agents/docs still uncommitted — user
  commits himself.
- vol_profile from tick files: fine; from kdb daily-only stores intraday
  table is required (documented).
- QBR notional column absent (run library stores %ADV not notional) —
  disclosed, not faked.

## 8. This chat's work blocks (sessions 6d–6g)

- **6d:** L6b (quantile LP + pooled ridge, gated) + Agent 13 "Shield
  (dark-patient)" + SOR/dark design narrative. 205→211 tests.
- **6e:** QBR module (agent + page 4 + demo quarter + 6 tests). 211.
- **6f:** kdb+/q source (agent1 refactor → assemble_market_data;
  kdb_source.py; Page-1 form; KDB_INTEGRATION.md; 8 tests). 219.
- **6g:** tick-file ingester (tick_ingest.py; 3-way source expander;
  free-source research; 10 tests). **229.**

## 9. Suggested next steps (not started)

- Wire L6b P10 forecast into Shield patience decision (listed in docs).
- Live A/B harness sketch for propagator calibration once real fills exist.
- QBR → pptx export (client deck) via pptx skill.
- LOBSTER real-file end-to-end walkthrough as demo video segment.
- Verify committed test_sell_side.py; commit the 6d–6g block.
