# HANDOFF v3 — Execution Analytics Platform (end of 2026-07-08)

*Supersedes `HANDOFF_2026-07-08_v2.md` (whose protocols §P-A…P-G still apply —
read them there; they encode real incidents). This records the state after the
2026-07-08 "session 5" marathon (5a–5k in `SESSION_SUMMARY_2026-07-08.md`,
which is the blow-by-blow; this is the digest).*

## 0. FIRST ACTIONS

1. `git status` — expect a LARGE uncommitted working set (30+ files: the whole
   day's work; the user reviews & commits, autopilot rule 4). HEAD `46a0289`.
   NOTE: HEAD's `tests/test_sell_side.py` contains a corrupted trailing line;
   the working tree carries the fix — it MUST be part of the commit.
2. Sandbox setup per v2 §5, then: `pytest -m "not live"` → **178 passed,
   1 deselected, no warnings**. AppTest smoke: all THREE pages render.
3. `.git` quirks seen today: corrupted index ("unknown index entry format")
   fixed via `git read-tree HEAD` (sandbox often cannot rm in .git); stale
   `index.lock` recurs after status — `rm -f .git/index.lock` (needs the
   Cowork delete permission once per folder).

## 1. What the platform is now (three pages)

- `app.py` is a 65-line dispatcher (B8 refactor); page bodies in `views/`
  (page1_simulator 1.6k lines / page2_rebalancing 671 / page3_program 83),
  shared imports in `views/common.py`. Package deliberately NOT named
  `pages/` (Streamlit auto-nav would fight the radio).
- **Page 1 — Execution Algorithm Simulator:** desk verdict banner + pre-trade
  report download + run library (predicted-vs-realized bias/MAE) → ticket
  (side/limit/window/cap/locate; window now binds the LIVE session too) →
  pipeline (Agents 1–9, 13) → algo wheel (Friedman+Nemenyi league table) →
  cost model (OLS, robust SEs, A/B-with-controls) → post-trade TCA
  (benchmarks, reversion, perm/temp, Perold IS attribution waterfall
  reconciling ±0.1bp, EMS order-detail, markout curve) → microstructure &
  client analytics (CS/AR/EDGE/Roll spreads — EDGE now IN the blend (median),
  Amihud, seasonality, price limits, one-pager) → live session (alerts,
  interventions, B4 volume run-rate re-forecast + completion projection).
- **Page 2 — Index Rebalancing Analysis:** Agent 12 feeds (MSCI parsed live
  today: real VEDL deletion walkthrough in the user manual) → event study
  (market model + Brown-Warner inference: CAR t column, ±1.96σ band) →
  insights (auction concentration, reversal, drift, flow, η, crowding score,
  expected-move bands, post-event liquidity/beta shift) → Agent 14 S1–S4
  frontier → trader pack (card / schedules CSV / conditional playbook) +
  best-ex record store → basket exception blotter. Event library
  (`data/event_library.json`) feeds thresholds once n≥3.
- **Page 3 — Program Trading Desk:** session board (15 markets, lunch/close
  phases), regulation reference (board lots, short regimes incl. Korea
  Mar-2025 resumption, circuit breakers, T+n settlement), program pre-trade
  blotter (lot/short/capacity flags, injectable fetcher), execution wave plan
  by UTC close, simulated EOD reconciliation report.

## 2. Regression anchors (NEVER break; all in tests/)

Everything from v2 §4 still pinned, PLUS: IS attribution reconciles ±0.1bp
(all 8 algos × both sides); S1 tracking ≡ 0 even with η>0 (auction fill IS
the print); live window-binding parity (windowed live TWAP == static exactly;
no fills outside window across interventions); wheel Nemenyi CD formula;
event-inference closed-form anchors; Roll recovers iid bounce (±10%) and is
undefined on trends; markout sell-mirror; crowding tiers; expected-move
bands; program-desk session phases at 03:00 UTC, lot/short/settlement logic;
run/event/best-ex library keyed dedupe. 178 offline tests.

## 3. Data & derived files

`data/{event_library,run_library,bestex_records}.json` are DERIVED (now
.gitignored — reversible). `index_changes_cache.json` remains tracked.
yfinance budget note: 5 fetch-pairs were used on 2026-07-08 (VEDL walkthrough
×2 + intraday); keep to 1–2/session (P-8).

## 4. Docs map (all in docs/)

User-facing: `Index_Rebalancing_User_Manual.docx` (v1.1),
`Trader_Features_Guide.docx`. Design/strategy: `TRADER_WORKFLOW_DESIGN`,
`REBALANCE_RESEARCH_AUTOMATION` (10 streams → features),
`INSTITUTIONAL_PLATFORM_PROPOSAL` (F1–F8, P1–P7),
`SIMULATOR_INSTITUTIONAL_ASSESSMENT` (P-A…P-F: all shipped except multi-day),
`QUANT_REVIEW_ADDITIONS` (stats+microstructure review),
`ARCHITECTURE_DIAGRAMS.md` (mermaid, canonical; extracted twins in
docs/diagrams/). Registers updated through today.

## 5. Backlog (prioritized, all with context in the docs above)

1. **B7 / I-10 — multi-day parent orders** (last big engine gap; unlocks
   realistic >1-day capacity, multi-day IS delay, program-desk RED names).
2. **Candidate radar** (research memo stream C): rank/buffer screens ahead of
   review dates; approximate float disclosed; hooks exist in Agent 12.
3. **Venue-level markouts** (join agent13 venue sim to the markout engine).
4. **Cost-model upgrades:** quantile regression (tail cost), OOS walk-forward
   validation; wheel-across-configs BH screen (note already in wheel output).
5. **Impact-decay propagator fit** (Bouchaud double-sqrt) on the reversion view.
6. **F6–F8 institutional items** (live imbalance feeds, distributional
   simulation once library n≥~15/market, one-click staging Page 2 → Page 1).
7. Signature plot (realized vol vs sampling interval) — cheap microstructure
   diagnostic, unclaimed.

## 6. Editing protocol updates

P-A now applies per-view: only `views/page1_simulator.py` (~1.6k lines)
demands bash-python anchor patching; other modules are safely small. Keep
new UI in views/, new analytics in agents/ (app.py stays a dispatcher).
pyflakes proved its worth today (caught a latent NameError silently disabled
by a try/except — the cost-model→scorecard bridge); run it after any
refactor-ish change: `python3 -m pyflakes app.py views/*.py agents/*.py`
(filter star-import noise).

## 7. The bar (unchanged, v2 §8)

Honest boundaries ARE the feature. Every number: cite the doc, the test, or
the disclosure. When in doubt between impressive and honest, choose honest.
